<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Register</title>

    <!-- Bootstrap Link -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

    <!-- Custom Styles -->
    <style>
        body {
            background-color: #f7814f;
            color: white;
            font-family: 'Arial', sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }

        form {
            width: 100%;
            max-width: 400px;
        }

        .btn-primary {
            background-color: #B23B3B;
            border: none;
            font-size: 1.2rem;
            padding: 12px 30px;
            border-radius: 8px;
            transition: background-color 0.3s ease;
        }

        .btn-primary:hover {
            background-color: #ad2a2a;
        }

        .form-control {
            border-radius: 10px;
            background-color: #fff;
            border: 1px solid #ccc;
        }

        .form-control:focus {
            border-color: #FAA73F;
            box-shadow: 0 0 5px rgba(250, 167, 63, 0.5);
        }

        h1 {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 20px;
            text-align: center;
        }

        .form-label {
            font-size: 1.1rem;
            font-weight: 600;
        }

        .text-center img {
            margin-bottom: 20px;
            width: 60%;
            height: auto;
            transition: opacity 0.3s ease;
        }

        .form-control:focus ~ .text-center img {
            opacity: 0.2;
        }

        .alert-success {
            background-color: #4caf50;
            color: white;
        }

        .alert-danger {
            background-color: #fa1504;
            color: white;
        }

        .login-link {
            display: block;
            text-align: center;
            margin-top: 15px;
            color: #fffefd;
            text-decoration: none;
            font-weight: bold;
        }

        .login-link:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <form action="/insertStudent" method="post" enctype="multipart/form-data">
        <!-- Logo -->
        <div class="text-center">
            <br><br><br><br>
            <img src="./Images/itm_logo.png" alt="ITM University Logo" class="rounded-circle">
        </div>

        <!-- Heading -->
        <h1>Register Here</h1>

        <!-- Message Alerts -->
        <% if(message.length > 0) { %>
        <div class="alert alert-danger">
            <%= message %>
        </div>
        <% } %>

        <% if(msg.length > 0) { %>
        <div class="alert alert-success">
            <%= msg %>
        </div>
        <% } %>

        <!-- User Name -->
        <div class="mb-4">
            <label for="name" class="form-label">Name</label>
            <input id="name" name="name" class="form-control text-capitalize" type="text" placeholder="Enter your name"
                required />
        </div>

        <!-- Email -->
        <div class="mb-3">
            <label for="email" class="form-label">Email</label>
            <input id="email" name="email" class="form-control" type="email" placeholder="name@example.com" required />
        </div>

        <!-- Password -->
        <div class="mb-4">
            <label for="password" class="form-label">Password</label>
            <input id="password" name="password" class="form-control" type="password" placeholder="Enter your password"
                required />
        </div>

        <!-- Confirm Password -->
        <div class="mb-4">
            <label for="confirmpassword" class="form-label">Confirm Password</label>
            <input id="confirmpassword" name="confirmpassword" class="form-control" type="password"
                placeholder="Re-enter your password" required />
        </div>

        <!-- Image Upload -->
        <div class="mb-4">
            <label for="image" class="form-label">Upload Image</label>
            <input id="image" name="image" class="form-control" type="file" required />
        </div>

        <!-- Register Button -->
        <div class="d-flex justify-content-center">
            <button type="submit" class="btn btn-primary btn-lg">Register</button>
        </div>

        <!-- Back to Login Link -->
        <a href="/" class="login-link">Back to Login</a>
    </form>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>
</body>

</html>
